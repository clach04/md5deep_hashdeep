This directory is for 'branches' to the SVN repository. We used this directory
for the experimental version4 branch, which was then moved to the trunk with a
'svn rm trunk' followed by a 'svn mv branches/version4 trunk'.

Currently this directory is not used.
